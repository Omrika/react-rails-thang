<%= react_components 'Record', { data: @records }%>

<!-- react_components is a helper method provided y the react-rails gem that recieves the name of the React component we want to render along with the data we want to pass into it -->
